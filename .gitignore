# ── AI 도구 캐시 (약 25MB) ─────────────────────────────
# 스킬·플러그인 캐시라 프로젝트 산출물이 아니다. 재설치로 복구된다.
.claude/
.agents/
.mswai/
.codex/
.github/
.serena/

# ── 플랫폼 제공 API 정의 ────────────────────────────────
# MSW Maker 가 월드마다 제공하는 넥슨 SDK 정의(.d.mlua).
# 읽기 전용이고 재배포 대상이 아니므로 저장소에 담지 않는다.
Environment/

# ── 기획 문서 · 스크린샷 (로컬 보관) ──────────────────────
# GDD / Phase 계획 / 캡처 이미지. 저장소는 코드만 담는다.
Docs/

# ── 🔴 자격증명 · 로컬 환경 설정 (절대 커밋 금지) ──────────
# .mcp.json 에는 Nexon MCP 서버 Bearer 토큰과 로컬 경로가 들어 있다.
.mcp.json
skills-lock.json

# ── 작업 중 만든 임시 스크립트 ──────────────────────────
tmp_*.js
build_ui.js

# ── 기타 ───────────────────────────────────────────────
.vscode/
Mislocated/
Thumbs.db
desktop.ini
.DS_Store

# 작업용 임시 스크립트
.builder-work/
